//props for letter and possibly the body???
import React from 'react';
import PropTypes from 'prop-types';

function Letter(props) {
	return (
		<>
		</>
	);
}

Letter.propTypes = {
	letterString: PropTypes.string
};

export default Letter;